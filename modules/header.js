import Element from "../constructor/Element.js";

const header = new Element('div', 'header', 'Header').get(); // создаем компонент header с тэгом, классом и текстовым содержимым

export default header;